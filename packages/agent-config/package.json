{
  "name": "@repo/agent-config",
  "private": true,
  "version": "0.0.0",
  "bin": {
    "agent-config": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "sync": "pnpm build && node dist/index.js sync",
    "check": "pnpm build && node dist/index.js sync --check",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "typescript": "^5.9.0"
  }
}
