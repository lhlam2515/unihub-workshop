/**
 * Injection token for {@link StorageConfig}.
 *
 * Used by `StorageModule.forRoot()` to register the configuration and by
 * `StorageService` to consume it.
 */
export const STORAGE_CONFIG = "STORAGE_CONFIG";
